# `rxp-pg` - Durable eXecution Primitives - PostgreSQL

`rxp` is a Go library providing low-level interfaces and primitives required by
Durable Execution platforms.

This repository, `rxp-pg` is an implementation of the [`rxp` library][rxp] on top of
PostgreSQL.

[rxp]: https://github.com/relexec/rxp
